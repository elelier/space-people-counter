# Cloudflare Pages configuration (optional)
# This file can be used for advanced configurations
# Most settings are auto-detected by Cloudflare Pages

name = "space-people-counter"
type = "javascript"

[env.production]
name = "space-people-counter"

# Build configuration
[build]
command = "npm run build"
cwd = "."
